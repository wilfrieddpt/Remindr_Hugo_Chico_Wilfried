<!-- main.handlebars -->

<!DOCTYPE html>
<html>
<head>
    <title>My Website</title>
    <style>
        body{
            margin:0;
        }
        header {
            background-color: rgb(30, 160, 110);
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-right: 10px;
            padding-left: 10px;
        }

        .user-info {
            display: flex;
            align-items: center;
        }

        .user-info a {
            margin-left: 10px;
            color : white;
            text-decoration : none;
        }

        .user-info p {
            margin-left: 10px;
            color : white;
            text-decoration : none;
        }


        .nav-links {
            display: flex;
        }

        .nav-links a {
            margin-right: 10px;
            color:white;
            text-decoration : none;
        }

        /*Cette partie concerne le menu déroulant pour la déconnexion*/
        .dropdown-menu {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
            z-index: 1;
        }

        .dropdown-menu a {
            color: black;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
        }

        .dropdown:hover .dropdown-menu {
            display: block;
        }
        /*Fin du menu déroulant*/
    </style>
</head>
<body>
    <header>
        <div class="nav-links">
            <a href="dashboard">Tableau de bord</a>
            <a href="addTask">Tache</a>
            <a href="Group">Groupe</a>
        </div>
        <div class="user-info">
            <div class="dropdown">
                <p class="dropdown-toggle">Nom de l'utilisateur connecté</p>
                <div class="dropdown-menu">
                    <a href="logout">Se déconnecter</a>
                </div>
            </div>
            <a href="login">Connexion</a>
            <a href="signup">Inscription</a>
        </div>
    </header>

    {{{body}}}

</body>
</html>
